package mytest;

public class W01Test {
    public static void main(String[] args) {
        String name = "사나";

        System.out.println(name);
        System.out.println(name.toString());
        // 문자열에서는 toString이 생략된 것이다.

        System.out.println();

    }

    class Student {
        private String name;
        private int age;

        public Student(String name, int age) {
            this.name = name;
            this.age = age;
        }

        

        @Override
        public String toString(name, age) {
            return (this.name="홍길동", this.age=20;
        }
        // toString() 메소드를 여기에 작성
    }
}
