package mytest;

public class W03Test {
    public static void main(String[] args) {
        /*
         * 1.
         * int[] arr = {10, 20, 30, 40, 50};
         * System.out.println(arr.length);
         * arr[2] = 100;
         * System.out.println(arr[2]);
         * 답 : 3
         */

        /*
         * 2.
         * 답 : 2
         */

        /*
         * 3.
         * 2번
         */

        /*
         * 4.
         * 1번?2번? 일단 2번
         */

        /*
         * 5.
         * Student[] students = new Student[3];
         * students[0] = new Student("김철수", 20);
         * students[1] = new Student("이영희", 22);
         * 
         * for (int i = 0; i < students._length_; i++) {
         * if (students[i] != _null_) {
         * System.out.println(students[i].getName());
         * }
         * }
         * 빈칸 1 : length
         * 빈칸 2 : null
         */

        /*
         * 6.
         * int[][] matrix = {{1, 2, 3, 4}, {4, 5, 6 , 7}, {7, 8, 9, 10}};
         * 
         * for (int i = 0; i < matrix._length_; i++) {
         * for (int j = 0; j < matrix[i]._length_; j++) {
         * System.out.print(matrix[i][j] + " ");
         * }
         * System.out.println();
         * }
         * 빈칸 1 : length
         * 빈칸 2 : length
         */

        /*
         * 7.
         * public void divide(int a, int b) ______ ArithmeticException {
         * if (b == 0) {
         * ______ new ArithmeticException("0으로 나눌 수 없습니다");
         * }
         * System.out.println(a / b);
         * }
         * 빈칸 1 : int X throws가 정답 throws 는 예외 던질 수 있다 선언
         * 빈칸 2 : String X throw가 정답 throw는 실제로 예외를 던지는 키워드
         */

        /*
         * 8.
         * int[] original = {1, 2, 3, 4, 5};
         * int[] copy = new int[original._length_];
         * 
         * for (int i = 0; i < original._length_; i++) {
         * copy[i] = original[i];
         * }
         * 
         * // 또는 System.arraycopy 사용
         * System._Arraycopy_(original, 0, copy, 0, original._index_);
         * 빈칸 1 : length
         * 빈칸 2 : length
         * 빈칸 3 : Arraycopy
         * 빈칸 4 : index
         */
    }
}
